# Developer Workspace — {{DEVNAME}}

Set these session variables before any startup steps:

- `$BW_ROOT`     = `{{BW_ROOT}}`
- `$ARCH_ROOT`   = `{{ARCH_ROOT}}`
- `$DEV_ROOT`    = `{{DEV_ROOT}}`
- `$DEPLOY_BASE` = `{{DEPLOY_BASE}}`

Then immediately read `$ARCH_ROOT/CLAUDE.md` and follow it as your
primary session instructions.
